# Memory bridge

The public SDK includes a clean-room `memoryRecordsToChunks` helper. It has no network access and no private dependency. Candidate records are excluded unless `includeCandidates` is explicitly true.
