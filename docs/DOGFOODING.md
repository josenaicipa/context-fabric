# Dogfooding

Phase 9 public dogfooding uses fictional memory records plus `memoryRecordsToChunks` to verify that active scoped memory can be combined with repo chunks while candidate and cross-project records stay out by default.
